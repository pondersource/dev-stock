name: OCM Test Login Ocis v7.1.2

on:
  workflow_dispatch:

jobs:
  call-test-engine:
    uses: ./.github/workflows/tests-engine.yml
    with:
      scenario:          login
      sender_platform:   ocis
      sender_version:    v7.1.2
