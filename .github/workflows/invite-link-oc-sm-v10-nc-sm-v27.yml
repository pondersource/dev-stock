name: OCM Test Invite link Owncloud-sm v10.15.0 to Nextcloud-sm v27.1.11

on:
  workflow_dispatch:

jobs:
  call-test-engine:
    uses: ./.github/workflows/tests-engine.yml
    with:
      scenario:          invite-link
      sender_platform:   owncloud-sm
      sender_version:    v10.15.0
      receiver_platform: nextcloud-sm
      receiver_version:  v27.1.11
